name: misarreach
description: Official Dart SDK for MisarReach — lead finder, multi-channel outreach, CRM, autopilot. Full API coverage with retry logic and SSE streaming.
version: 1.0.0
homepage: https://misarreach.com/docs
documentation: https://misarreach.com/docs
issue_tracker: https://misarreach.com/contact

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.2.0

dev_dependencies:
  test: ^1.25.0
  lints: ^3.0.0
