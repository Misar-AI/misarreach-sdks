name: misar_reach_flutter
description: Official Flutter SDK for MisarReach — secure key storage, retry, SSE streaming, and full API coverage for iOS, Android, macOS, Web.
version: 5.0.1
homepage: https://misarreach.com/docs
documentation: https://misarreach.com/docs
issue_tracker: https://misarreach.com/contact

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # No assets or fonts needed for SDK
